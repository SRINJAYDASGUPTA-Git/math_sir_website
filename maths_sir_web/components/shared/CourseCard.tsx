import React from 'react'

const CourseCard = ({
    image, 
    id,
    title,
}:{
    image: string,
    id: string,
    title: string,
}) => {
  return (
    <div>CourseCard</div>
  )
}

export default CourseCard